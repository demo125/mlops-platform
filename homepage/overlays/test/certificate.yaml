apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homepage-cert
spec:
  dnsNames:
  - homepage.localhost
  commonName: "my-selfsigned-cert"
  secretName: homepage-cert
  issuerRef:
    name: homepage-issuer
