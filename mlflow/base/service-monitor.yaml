apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app: mlflow
    release: mlflow
  name: mlflow-servicemonitor
spec:
  endpoints:
  - interval: 15s
    port: http
  selector:
    matchLabels:
      app: mlflow
      heritage: Helm
      release: mlflow
