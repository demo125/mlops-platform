apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mlflow-minio
  labels:
    app: mlflow
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: minio
            port:
              number: 9001
  - http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: minio
            port:
              number: 9000
    host: mlflow-minio-backend.localhost
  tls:
  - hosts:
    - mlflow-minio.localhost
    - mlflow-minio-backend.localhost
    secretName: mlflow-minio-selfsigned-cert
