apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus-selfsigned-cert
spec:
  dnsNames:
  - prometheus.localhost
  commonName: "my-selfsigned-cert"
  secretName: prometheus-selfsigned-cert
  issuerRef:
    name: prometheus-issuer
