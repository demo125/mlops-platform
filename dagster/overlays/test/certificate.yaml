apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dagster-dagster-webserver-cert
spec:
  dnsNames:
  - dagster.localhost
  commonName: "my-selfsigned-cert"
  secretName: dagster-dagster-webserver-cert
  issuerRef:
    name: dagster-issuer
